#注册到eureka的服务地址
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:6868/eureka/
  instance:
    preferIpAddress : true
    instance-id: ${spring.cloud.client.ip-address}:${spring.application.name}:${server.port}
#服务配置
server:
  port: 9005
#spring配置
spring:
  #1.应用配置
  application:
    name: ihrm-attendance #指定服务名
  #2.数据库连接池
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: ${IHRM_MYSQL_URL:jdbc:mysql://127.0.0.1:3306/ihrm?useUnicode=true&characterEncoding=utf8&useSSL=false}
    username: ${IHRM_MYSQL_USERNAME:root}
    password: ${IHRM_MYSQL_PASSWORD:}
    hikari:
      maximum-pool-size: 2
  redis:
    host: ${IHRM_REDIS_HOST:127.0.0.1}
    port: ${IHRM_REDIS_PORT:6379}
  #3.JPA
  jpa:
    database: MySQL
    show-sql: true
    open-in-view: true
atte:
  holidays: 20191001,20191002,20191003,20190101
  wordingDays: 20190927
